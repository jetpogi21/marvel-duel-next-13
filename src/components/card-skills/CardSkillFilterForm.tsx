//Generated by WriteToModelfilterform_tsx
"use client";
import { useCardSkillStore } from "@/hooks/card-skills/useCardSkillStore";
import {
  CardSkillFormikFilter,
  CardSkillSearchParams,
} from "@/interfaces/CardSkillInterfaces";
import { DEFAULT_LIMIT } from "@/utils/constants";
import { getFilterValueFromURL, getParamsObject } from "@/utils/utilities";
import { encodeParams } from "@/utils/utils";
import { Form, Formik, FormikHelpers, FormikProps } from "formik";
import React from "react";
import { DEFAULT_FILTERS } from "@/utils/constants/CardSkillConstants";
import LimitSelector from "@/components/form/LimitSelector";
import { useURL } from "@/hooks/useURL";
import FormikControl from "@/components/form/FormikControl";

const CardSkillFilterForm: React.FC = () => {
  const { router, query, pathname } = useURL<CardSkillSearchParams>();

  //SearchParams Variables
  const limit = query["limit"] || DEFAULT_LIMIT;

  const defaultFilters = DEFAULT_FILTERS;
  const initialValues: CardSkillFormikFilter = getFilterValueFromURL(
    query,
    defaultFilters
  );

  //Zustand stores
  const [setPage, setLastPage, setFetchCount, resetRowSelection] = useCardSkillStore(
    (state) => [
      state.setPage,
      state.setLastPage,
      state.setFetchCount,
      state.resetRowSelection,
    ]
  );

  const handleFormikSubmit = (
    values: Partial<CardSkillFormikFilter>,
    formik: FormikHelpers<CardSkillFormikFilter>
  ) => {
    const params = {
      ...query,
      ...(getParamsObject(values, defaultFilters) as Partial<CardSkillSearchParams>),
    };

    const newURL = `${pathname}?${encodeParams(params)}`;
    router.push(newURL);
    setFetchCount(true);
    setPage(1);
    setLastPage(1);
    resetRowSelection();
  };

  const handleLimitChange = (value: string) => {
    const params = { ...query, limit: value };
    const newURL = `${pathname}?${encodeParams(params)}`;
    router.push(newURL);
    resetRowSelection();
  };

  const renderFormik = (formik: FormikProps<CardSkillFormikFilter>) => {
    return (
      <Form className="flex gap-4" autoComplete="off">
        {/* Generated by GetFormikFilterQControl */}<FormikControl
  name="q"
  submitOnChange={true}
  placeholder="Filter Card Skills..."
  type="Text"
/>
        {/* Generated by GetAllFormikFilterControlBySeqModel */}
      </Form>
    );
  };

  return (
    <div className="flex justify-between w-full">
      <Formik
        initialValues={initialValues}
        onSubmit={handleFormikSubmit}
      >
        {renderFormik}
      </Formik>
      <LimitSelector
        handleLimitChange={handleLimitChange}
        value={limit}
      />
    </div>
  );
};

export default CardSkillFilterForm;
