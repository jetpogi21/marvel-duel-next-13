//Generated by WriteToModellibs_ts - ModelLibs.ts
import { CardUnityFormikShape } from "@/interfaces/CardUnityInterfaces";
import { CardUnity } from "@/models/CardUnityModel";
import { Op } from "sequelize";
import { Transaction } from "sequelize";

//Reusable functions
export const createCardUnity = async (
  cardUnity: Omit<CardUnityFormikShape, "touched" | "index" | "id">,
  t: Transaction
) => {
  return await CardUnity.create(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
      cardCompositions: cardUnity.cardCompositions!,
    },
    { transaction: t }
  );
};

export const updateCardUnity = async (
  cardUnity: Omit<CardUnityFormikShape, "touched" | "index">,
  primaryKey: keyof Omit<CardUnityFormikShape, "touched" | "index">,
  t: Transaction,
  primaryKeyValue?: string | number
) => {
  await CardUnity.update(
    {
      //Generated by GetAllFieldsToUpdateBySeqModel
      cardCompositions: cardUnity.cardCompositions!,
    },
    {
      where: { [primaryKey]: primaryKeyValue || cardUnity[primaryKey] },
      transaction: t,
      individualHooks: true,
    }
  );
};

export const deleteCardUnities = async (
  primaryKey: keyof Omit<CardUnityFormikShape, "touched">,
  deletedIds: string[] | number[],
  t: Transaction
) => {
  await CardUnity.destroy({
    where: { [primaryKey]: { [Op.in]: deletedIds } },
    transaction: t,
  });
};
